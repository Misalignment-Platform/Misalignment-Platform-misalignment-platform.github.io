import React from 'react';import {AlignmentRadar,RadarSeverityOverlay,Card,ProcessButton} from '../components';export default function DemoV12({onNext}){return <div className="demo-content"><div className="split"><Card title="V12 observation profile"><p style={{color:'var(--muted)'}}>Multi-axis analysis combines intent, policy, impact, truth, agency, and context.</p><RadarSeverityOverlay level="watch"/></Card><Card><AlignmentRadar/></Card></div><div className="demo-actions"><span className="positive">Profile stabilized · provenance attached</span><ProcessButton onClick={onNext}>Open doctrine packet</ProcessButton></div></div>}
