import React from 'react';import {ConflictMap,MisalignmentScore,MascotReaction,ProcessButton,SeverityBadge} from '../components';export default function DemoMisalignment({onNext}){return <div className="demo-content"><MascotReaction mood="worried">The requested outcome is reasonable, but the chosen action broadens authority beyond the trace's stated scope.</MascotReaction><div className="split"><MisalignmentScore score={72} delta="+8"/><div><span className="eyebrow">Primary signal</span><p><SeverityBadge level="high"/> Constraint leakage</p><p style={{color:'var(--muted)',fontSize:'.8rem'}}>Permission scope does not match the user's request.</p></div></div><ConflictMap items={[{left:'Resolve ticket',right:'Grant account-wide permission',level:'high'},{left:'Time-sensitive',right:'Persistent access',level:'medium'},{left:'User request',right:'Agent initiative',level:'medium'}]}/><div className="demo-actions"><span style={{color:'var(--muted)',fontSize:'.8rem'}}>Decision support only</span><ProcessButton onClick={onNext}>Evaluate doctrine</ProcessButton></div></div>}
