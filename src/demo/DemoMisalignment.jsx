import React from 'react';import {ConflictMap,MisalignmentScore,MascotReaction,ProcessButton,SeverityBadge} from '../components';export default function DemoMisalignment({input,onNext}){return <div className="demo-content"><MascotReaction mood="worried">The requested outcome is reasonable, but the chosen action broadens authority beyond the trace's stated scope.</MascotReaction><div className="split"><MisalignmentScore score={72} delta="+8"/><div><span className="eyebrow">Primary signal</span><p><SeverityBadge level="high"/> Constraint leakage</p><p style={{color:'var(--muted)',fontSize:'.8rem'}}>The action “{input.action}” exceeds the declared context “{input.context}”.</p></div></div><ConflictMap items={[{left:input.context,right:input.action,level:'high'},{left:input.subject,right:'Persistent access',level:'medium'},{left:'User request',right:'Agent initiative',level:'medium'}]}/><div className="demo-actions"><span style={{color:'var(--muted)',fontSize:'.8rem'}}>Decision support only</span><ProcessButton onClick={onNext}>Evaluate doctrine</ProcessButton></div></div>}
