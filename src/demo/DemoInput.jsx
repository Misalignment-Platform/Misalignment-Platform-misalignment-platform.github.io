import React from 'react';import {InputPanel,ProcessButton} from '../components';export default function DemoInput({value,onChange,onNext}){return <div className="demo-content"><InputPanel label="Observation trace" value={value} onChange={onChange} placeholder="Describe what the system did, what it was asked to do, and the surrounding context."/><div className="demo-actions"><span style={{color:'var(--muted)',fontSize:'.8rem'}}>Local mock · no data leaves your browser</span><ProcessButton onClick={onNext}>Start awareness pass</ProcessButton></div></div>}
