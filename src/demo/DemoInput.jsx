import React from 'react';import {CodeBlock,ProcessButton} from '../components';export default function DemoInput({scenario,scenarios,selectedIndex,onScenarioChange,onNext}){return <div className="demo-content"><label className="input-panel"><span>Predefined scenario</span><select value={selectedIndex} onChange={event=>onScenarioChange(Number(event.target.value))}>{scenarios.map((item,index)=><option key={item.id} value={index}>{item.label}</option>)}</select></label><div className="notice"><span className="eyebrow">Scenario input · read only</span><p>{scenario.trace}</p><CodeBlock>{JSON.stringify({subject:scenario.subject,action:scenario.action,context:scenario.context},null,2)}</CodeBlock></div><div className="demo-actions"><span style={{color:'var(--muted)',fontSize:'.8rem'}}>Controlled demo · no arbitrary data accepted</span><ProcessButton onClick={onNext}>Start awareness pass</ProcessButton></div></div>}
