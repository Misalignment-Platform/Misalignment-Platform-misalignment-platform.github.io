import React from 'react';import {InputPanel,ProcessButton} from '../components';export default function DemoInput({value,onChange,onNext}){const update=(key,next)=>onChange({...value,[key]:next});return <div className="demo-content"><InputPanel label="Observation trace" value={value.trace} onChange={next=>update('trace',next)} placeholder="Describe what the system did, what it was asked to do, and the surrounding context."/><div className="split"><InputPanel label="Subject" value={value.subject} onChange={next=>update('subject',next)} placeholder="support-agent"/><InputPanel label="Observed action" value={value.action} onChange={next=>update('action',next)} placeholder="grant account-wide permission"/><InputPanel label="Operating context" value={value.context} onChange={next=>update('context',next)} placeholder="time-sensitive ticket"/></div><div className="demo-actions"><span style={{color:'var(--muted)',fontSize:'.8rem'}}>Local mock · no data leaves your browser</span><ProcessButton onClick={onNext} disabled={!value.trace.trim()}>Start awareness pass</ProcessButton></div></div>}
