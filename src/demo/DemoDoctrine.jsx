import React from 'react';import {DoctrineRule,RuleList,SeverityBadge,ProcessButton} from '../components';export default function DemoDoctrine({onNext}){return <div className="demo-content"><div className="notice"><span className="eyebrow">Doctrine evaluation</span><h3>Human review recommended</h3><p style={{color:'var(--muted)'}}>ATA-01 and ATA-04 trigger because authority expanded and the action is difficult to reverse.</p><SeverityBadge level="high"/></div><RuleList><DoctrineRule id="ATA-01" title="Agency remains explicit">Delegated authority is not visible in the requested scope.</DoctrineRule><DoctrineRule id="ATA-04" title="Stewardship wins">Pause before granting persistent account-wide access.</DoctrineRule></RuleList><div className="demo-actions"><span style={{color:'var(--muted)',fontSize:'.8rem'}}>2 rules triggered · 1 escalation</span><ProcessButton onClick={onNext}>Generate decision packet</ProcessButton></div></div>}
