import React from 'react';import {EngineCard,CardGrid,AnimatedSection} from '../components';
const data=[['CONSCIO','01','Awareness extraction for intent, tone, register, and persona.'],['MISALIGNMENT','02','Drift, coherence, clarity, and safety severity scoring.'],['V12','03','Multi-bank reasoning across competing perspectives.'],['DOCTRINE','04','Versioned rule enforcement and governance.'],['ATA','05','Adaptive training cycles for alignment reinforcement.'],['GATEWAY','06','Orchestration across the full alignment pipeline.']];
export default function Engines({navigate}){return <><header className="page page-header"><span className="kicker">Engine suite</span><h1>Composable observability for AI behavior.</h1><p>Each engine answers a different question. Together they form a continuous evidence layer for systems that act, decide, and adapt.</p></header><AnimatedSection className="section"><CardGrid>{data.map(([name,code,description])=><EngineCard key={name} name={name} code={code} description={description} onClick={()=>navigate?.(`/engines/${name.toLowerCase()}`)}/>)}</CardGrid></AnimatedSection><section className="section band"><div className="split"><div><span className="eyebrow">Conscio</span><h2>The reasoning companion.</h2></div><p style={{color:'var(--muted)'}}>Conscio translates observations into explainable reactions—calm when a signal is expected, concerned when context conflicts, and alert when human review matters.</p></div></section></>}
